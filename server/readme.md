# Project Blue Ocean Starter Code

Express 5 API server for Project Blue Ocean.

## Scripts

```bash
npm install   # install dependencies
npm start     # start the server on PORT (default 8000)
npm test      # run the test suite (node:test + supertest)
```

## Routes

- `GET /api/example` — returns `{ data: [...] }` of sea creatures.